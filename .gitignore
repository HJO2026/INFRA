# macOS
.DS_Store

# 측정 결과 원본. 리포트 md만 커밋 허용
results/**
!results/.gitkeep
!results/**/
!results/**/report.md

# 로컬 오버라이드·비밀값 (예시는 .env.example)
compose/compose.local.yml
.env
.env.local

# Claude Code 로컬 설정 (커밋하지 않는다. 각자 로컬에서만 사용)
.claude/
CLAUDE.md
CLAUDE.local.md
